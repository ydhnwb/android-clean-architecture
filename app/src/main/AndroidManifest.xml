<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ydhnwb.cleanarchitectureexercise">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".presentation.main.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CleanArchitectureExercise">
        <activity
            android:theme="@style/Theme.CleanArchitectureExercise.NoActionBar"
            android:name=".presentation.register.RegisterActivity">

        </activity>
        <activity
            android:name=".presentation.login.LoginActivity"
            android:theme="@style/Theme.CleanArchitectureExercise.NoActionBar">
        </activity>
        <activity
            android:name=".presentation.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.CleanArchitectureExercise.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>